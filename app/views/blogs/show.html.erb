<p id="notice"><%= notice %></p>
<div class="page-header">
<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>
</div>
<p>
	<%== @blog.created_at.strftime("%B, %d, %Y") %><br>
	<%== @blog.body %>
</p>

<%= link_to 'Edit', edit_blog_path(@blog) %> |
<%= link_to 'Back', blogs_path %>
